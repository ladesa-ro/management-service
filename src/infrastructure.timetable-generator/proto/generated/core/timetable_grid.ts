// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v3.21.12
// source: core/timetable_grid.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { TimeSlot } from "./time_slot";
import { TimetableGridSchedule } from "./timetable_grid_schedule";

export const protobufPackage = "ladesa.timetable_generator.v1.core";

export interface TimetableGrid {
  dateStart: string;
  dateEnd: string;
  timeSlots: TimeSlot[];
  schedules: TimetableGridSchedule[];
}

function createBaseTimetableGrid(): TimetableGrid {
  return { dateStart: "", dateEnd: "", timeSlots: [], schedules: [] };
}

export const TimetableGrid: MessageFns<TimetableGrid> = {
  encode(message: TimetableGrid, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dateStart !== "") {
      writer.uint32(10).string(message.dateStart);
    }
    if (message.dateEnd !== "") {
      writer.uint32(18).string(message.dateEnd);
    }
    for (const v of message.timeSlots) {
      TimeSlot.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.schedules) {
      TimetableGridSchedule.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TimetableGrid {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTimetableGrid();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dateStart = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.dateEnd = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.timeSlots.push(TimeSlot.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.schedules.push(TimetableGridSchedule.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TimetableGrid {
    return {
      dateStart: isSet(object.dateStart)
        ? globalThis.String(object.dateStart)
        : isSet(object.date_start)
          ? globalThis.String(object.date_start)
          : "",
      dateEnd: isSet(object.dateEnd)
        ? globalThis.String(object.dateEnd)
        : isSet(object.date_end)
          ? globalThis.String(object.date_end)
          : "",
      timeSlots: globalThis.Array.isArray(object?.timeSlots)
        ? object.timeSlots.map((e: any) => TimeSlot.fromJSON(e))
        : globalThis.Array.isArray(object?.time_slots)
          ? object.time_slots.map((e: any) => TimeSlot.fromJSON(e))
          : [],
      schedules: globalThis.Array.isArray(object?.schedules)
        ? object.schedules.map((e: any) => TimetableGridSchedule.fromJSON(e))
        : [],
    };
  },

  toJSON(message: TimetableGrid): unknown {
    const obj: any = {};
    if (message.dateStart !== "") {
      obj.dateStart = message.dateStart;
    }
    if (message.dateEnd !== "") {
      obj.dateEnd = message.dateEnd;
    }
    if (message.timeSlots?.length) {
      obj.timeSlots = message.timeSlots.map((e) => TimeSlot.toJSON(e));
    }
    if (message.schedules?.length) {
      obj.schedules = message.schedules.map((e) => TimetableGridSchedule.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TimetableGrid>, I>>(base?: I): TimetableGrid {
    return TimetableGrid.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TimetableGrid>, I>>(object: I): TimetableGrid {
    const message = createBaseTimetableGrid();
    message.dateStart = object.dateStart ?? "";
    message.dateEnd = object.dateEnd ?? "";
    message.timeSlots = object.timeSlots?.map((e) => TimeSlot.fromPartial(e)) || [];
    message.schedules = object.schedules?.map((e) => TimetableGridSchedule.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
