// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v3.21.12
// source: core/diary.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "ladesa.timetable_generator.v1.core";

export interface Diary {
  id: string;
  groupId: string;
  teacherId: string;
  subjectId: string;
  weekLimit: number;
  remaining: number;
  roomId?: string | undefined;
}

function createBaseDiary(): Diary {
  return {
    id: "",
    groupId: "",
    teacherId: "",
    subjectId: "",
    weekLimit: 0,
    remaining: 0,
    roomId: undefined,
  };
}

export const Diary: MessageFns<Diary> = {
  encode(message: Diary, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.groupId !== "") {
      writer.uint32(18).string(message.groupId);
    }
    if (message.teacherId !== "") {
      writer.uint32(26).string(message.teacherId);
    }
    if (message.subjectId !== "") {
      writer.uint32(34).string(message.subjectId);
    }
    if (message.weekLimit !== 0) {
      writer.uint32(40).int32(message.weekLimit);
    }
    if (message.remaining !== 0) {
      writer.uint32(48).int32(message.remaining);
    }
    if (message.roomId !== undefined) {
      writer.uint32(58).string(message.roomId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Diary {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDiary();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.groupId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.teacherId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.subjectId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.weekLimit = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.remaining = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.roomId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Diary {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      groupId: isSet(object.groupId)
        ? globalThis.String(object.groupId)
        : isSet(object.group_id)
          ? globalThis.String(object.group_id)
          : "",
      teacherId: isSet(object.teacherId)
        ? globalThis.String(object.teacherId)
        : isSet(object.teacher_id)
          ? globalThis.String(object.teacher_id)
          : "",
      subjectId: isSet(object.subjectId)
        ? globalThis.String(object.subjectId)
        : isSet(object.subject_id)
          ? globalThis.String(object.subject_id)
          : "",
      weekLimit: isSet(object.weekLimit)
        ? globalThis.Number(object.weekLimit)
        : isSet(object.week_limit)
          ? globalThis.Number(object.week_limit)
          : 0,
      remaining: isSet(object.remaining) ? globalThis.Number(object.remaining) : 0,
      roomId: isSet(object.roomId)
        ? globalThis.String(object.roomId)
        : isSet(object.room_id)
          ? globalThis.String(object.room_id)
          : undefined,
    };
  },

  toJSON(message: Diary): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.groupId !== "") {
      obj.groupId = message.groupId;
    }
    if (message.teacherId !== "") {
      obj.teacherId = message.teacherId;
    }
    if (message.subjectId !== "") {
      obj.subjectId = message.subjectId;
    }
    if (message.weekLimit !== 0) {
      obj.weekLimit = Math.round(message.weekLimit);
    }
    if (message.remaining !== 0) {
      obj.remaining = Math.round(message.remaining);
    }
    if (message.roomId !== undefined) {
      obj.roomId = message.roomId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Diary>, I>>(base?: I): Diary {
    return Diary.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Diary>, I>>(object: I): Diary {
    const message = createBaseDiary();
    message.id = object.id ?? "";
    message.groupId = object.groupId ?? "";
    message.teacherId = object.teacherId ?? "";
    message.subjectId = object.subjectId ?? "";
    message.weekLimit = object.weekLimit ?? 0;
    message.remaining = object.remaining ?? 0;
    message.roomId = object.roomId ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
