{
  "scripts": {
    "tsp:format": "nx tsp:format",
    "json-schema:generate": "nx json-schema:generate",
    "typescript:generate": "nx typescript:generate",
    "generate": "nx json-schema:generate && nx typescript:generate"
  },
  "devDependencies": {
    "@typespec/compiler": "^1.0.0",
    "@typespec/json-schema": "^1.0.0",
    "quicktype": "^23.2.6"
  }
}